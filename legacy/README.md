# Legacy Components

The files in this directory are retained for comparison and cookie-bridge
testing. They are not installable Home Assistant integrations in the public
package because they are outside `custom_components/`.

Use `custom_components/generac/` for the maintained integration.
